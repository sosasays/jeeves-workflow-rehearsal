# Jeeves workflow rehearsal

This repository exercises agent-driven pull requests, independent review, isolated CI and deployment. It uses synthetic fixtures and contains no research code, private history or market data.

Run `python3 -m unittest discover -s checks` for the CLI checks.
